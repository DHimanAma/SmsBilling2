<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SMSApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="defaultButton" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">@drawable/rounded_button_with_ripple_effect</item>
        <item name="android:textColor">@drawable/rounded_button_with_ripple_text_selector</item>
        <item name="android:textDirection" tools:targetApi="jelly_bean_mr1">locale</item>
        <item name="android:textAllCaps">false</item>

    </style>

    <style name="otpVerificationEditText" parent="TextFontAppearance">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">@dimen/_32sdp</item>
        <item name="android:gravity">center</item>
        <item name="android:cursorVisible">true</item>
        <item name="android:maxLength">1</item>
        <item name="android:textColor">@color/grey</item>
        <item name="android:inputType">phone</item>
        <item name="android:focusable">true</item>
        <item name="android:itemBackground">#65EAEAEA</item>
        <item name="android:focusableInTouchMode">true</item>
    </style>
    <style name="TextFontAppearance" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textDirection" tools:targetApi="jelly_bean_mr1">locale</item>
    </style>
</resources>